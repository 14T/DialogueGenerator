---
description: Superseded — see .claude/commands/auto-process.md (run via /auto-process).
---

This workflow has been merged into `.claude/commands/auto-process.md` (invoke as `/auto-process`). All steps, including the variant-check step that used to live only here, now live in that single file. This file is kept only as a pointer so old references don't break.
